module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['InterVariable', 'Inter', 'sans-serif'],
      },
      // (We’ll tune colors here soon)
    },
  },
  // ...rest of config
}
