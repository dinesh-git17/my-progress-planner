{
  "scripts": {
    "dev": "node scripts/dev-with-network.js",
    "dev:simple": "next dev -H 0.0.0.0",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "format": "prettier --write .",
    "kill-cache": "node scripts/kill-cache.js",
    "fresh-dev": "npm run kill-cache && npm run dev",
    "clean": "rm -rf .next && npm run kill-cache",
    "dev:clean": "npm run clean && npm run dev",
    "build:clean": "npm run clean && npm run build",
    "test:pwa": "npm run build && npm run start",
    "// Environment Management": "// Environment switching scripts",
    "env:test": "cp .env.test.local .env.local && echo '🧪 Switched to TEST environment (Mock GPT)' && node scripts/check-environment.js",
    "env:prod": "cp .env.production.local .env.local && echo '🚀 Switched to PRODUCTION environment (Real GPT)' && node scripts/check-environment.js",
    "env:check": "node scripts/check-environment.js",
    "// Test Environment Scripts": "// Development with mock GPT",
    "dev:test": "npm run env:test && npm run dev",
    "dev:test:clean": "npm run env:test && npm run clean && npm run dev",
    "dev:test:fresh": "npm run env:test && npm run kill-cache && npm run dev",
    "dev:test:ultra": "npm run env:test && npm run clean && npm run kill-cache && npm run dev",
    "// Production Environment Scripts": "// Development with real GPT",
    "dev:prod": "npm run env:prod && npm run dev",
    "dev:prod:clean": "npm run env:prod && npm run clean && npm run dev",
    "dev:prod:fresh": "npm run env:prod && npm run kill-cache && npm run dev",
    "dev:prod:ultra": "npm run env:prod && npm run clean && npm run kill-cache && npm run dev",
    "// Build Scripts": "// Build for different environments",
    "build:test": "npm run env:test && npm run build:clean",
    "build:prod": "npm run env:prod && npm run build:clean",
    "// Quick Actions": "// Most commonly used commands",
    "test": "npm run dev:test:fresh",
    "prod": "npm run dev:prod:fresh",
    "// GPT Environment Info": "// Check which GPT mode is active",
    "gpt:status": "node scripts/check-environment.js",
    "gpt:mock": "npm run env:test",
    "gpt:real": "npm run env:prod"
  }
}
